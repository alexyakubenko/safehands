services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/app/data/safehands.db"
      ADMIN_USER: "admin"
      ADMIN_PASSWORD: "safehands"
      PUBLIC_BASE_URL: "http://localhost:3000"
    volumes:
      - ./data:/app/data
